# Public site URL (no trailing slash). Used for Open Graph and Twitter absolute URLs.
# Required for correct previews when not on Vercel (local builds, custom hosts).
NEXT_PUBLIC_SITE_URL=https://your-production-domain.com
